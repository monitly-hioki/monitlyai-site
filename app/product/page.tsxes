import React from "react";

export default function ProductPage() {
  const features = [
    {
      img: "/product/dashboard-ui.png",
      title: "統合ダッシュボード",
      text: "開発・運用・評価の指標とイベントを一元可視化。意思決定のスピードと再現性を高めます。"
    },
    {
      img: "/product/performance-ui.png",
      title: "品質・性能モニタリング",
      text: "回答品質、レイテンシ、利用動向を自動収集。劣化や逸脱を即時に検知し、影響範囲を追跡します."
    },
    {
      img: "/product/compare-ui.png",
      title: "構成比較・A/Bテスト",
      text: "プロンプト、モデル、RAG構成を定量比較。勝ちパターンを素早く見極め改善の打ち手を明確化します。"
    },
    {
      img: "/product/review-workflow.png",
      title: "レビュー・評価ワークフロー",
      text: "SME評価を標準化。属人的判断を排し、共通指標でレビューを回すことで品質の底上げを実現します。"
    },
    {
      img: "/product/ops-alerts.png",
      title: "運用監視・アラート",
      text: "本番ログを常時監視し、出力の異常やガバナンス逸脱を検知。サービス継続性とコンプライアンスを担保します。"
    },
    {
      img: "/product/improvement-history.png",
      title: "改善履歴・トレーサビリティ",
      text: "変更と結果の履歴を自動蓄積。どの変更がどの指標に効いたかを追跡し、継続的最適化の循環を作ります。"
    }
  ];

  const benefits = [
    {
      title: "スピードと品質の両立",
      text: "比較検証と評価を仕組み化。変更効果を定量で確認し、改善サイクルを高速化。"
    },
    {
      title: "再現可能な意思決定",
      text: "共通指標と共通画面で合意形成。判断の再現性が高まり、属人化を排除。"
    },
    {
      title: "日本企業の要件に最適",
      text: "SaaSとオンプレミスに対応。権限管理・監査ログ・データ分離など社内規程へ適合。"
    }
  ];

  const steps = [
    "現状整理（対象機能とKPIの決定）",
    "接続設定（本番ログ・評価データの連携）",
    "監視としきい値設定（重要指標から開始）",
    "比較運用の開始（週次で勝ち案を反映）",
    "月次レビュー（棚卸しと次の打ち手合意）"
  ];

  return (
    <main className="bg-white text-zinc-800">
      <section className="px-6 md:px-10 pt-12 pb-14 bg-gradient-to-b from-zinc-50 to-white">
        <div className="max-w-6xl mx-auto text-center">
          <h1 className="text-3xl md:text-5xl font-bold tracking-tight">生成AIを測り、良くし、守る。</h1>
          <p className="mt-6 text-zinc-600 text-lg leading-relaxed">
            Monitlyは、RAG・AIエージェント・LLMの本番品質を継続改善するための運用評価プラットフォーム。
            トップページが全体像を語るのに対し、本ページでは機能・導入・運用の実務に踏み込みます。
          </p>
          <div className="mt-8">
            <a href="/contact" className="inline-block px-6 py-3 bg-black text-white rounded-xl text-sm font-medium">デモ・相談を申し込む</a>
          </div>
        </div>
      </section>

      <section className="px-6 md:px-10 py-16 bg-zinc-50">
        <div className="max-w-6xl mx-auto space-y-16">
          {features.map((f, i) => (
            <div key={f.title} className="md:grid md:grid-cols-2 gap-10 items-center">
              <div className={i % 2 === 1 ? "md:order-2" : ""}>
                <img src={f.img} alt={f.title} className="rounded-2xl shadow-md ring-1 ring-zinc-200 w-full h-auto" />
              </div>
              <div>
                <h3 className="text-2xl font-semibold">{f.title}</h3>
                <p className="mt-4 text-zinc-700 leading-relaxed">{f.text}</p>
              </div>
            </div>
          ))}
        </div>
      </section>

      <section className="px-6 md:px-10 py-16 bg-white">
        <div className="max-w-6xl mx-auto">
          <h2 className="text-3xl font-semibold text-center">導入メリット</h2>
          <div className="mt-10 grid md:grid-cols-3 gap-6">
            {benefits.map(b => (
              <div key={b.title} className="p-6 bg-white rounded-2xl shadow-sm ring-1 ring-zinc-200">
                <h3 className="text-lg font-semibold">{b.title}</h3>
                <p className="mt-2 text-zinc-700">{b.text}</p>
              </div>
            ))}
          </div>
        </div>
      </section>

      <section className="px-6 md:px-10 py-16 bg-white">
        <div className="max-w-6xl mx-auto grid md:grid-cols-2 gap-10">
          <div>
            <h2 className="text-2xl md:text-3xl font-semibold">提供形態とセキュリティ</h2>
            <ul className="mt-6 space-y-2 text-zinc-700 list-disc list-inside">
              <li>国内リージョンでの暗号化・分離運用</li>
              <li>アクセス制御・権限管理・監査ログを標準実装</li>
              <li>SaaSとオンプレミス（オンプレ）の両提供に対応</li>
            </ul>
          </div>
          <div>
            <h2 className="text-2xl md:text-3xl font-semibold">主なユースケース</h2>
            <ul className="mt-6 space-y-2 text-zinc-700 list-disc list-inside">
              <li>社内QA/ナレッジ検索、文書レビュー支援</li>
              <li>問い合わせ回答の標準化、コール要約、メール支援</li>
              <li>業務横断エージェントの評価・監視・継続改善</li>
            </ul>
          </div>
        </div>
      </section>

      {/* 導入ステップ & 成果の出し方 */}
<section className="px-6 md:px-10 py-16 bg-white">
  <div className="max-w-6xl mx-auto grid md:grid-cols-2 gap-10">
    <div>
      <h2 className="text-2xl md:text-3xl font-semibold">導入ステップ</h2>
      <ol className="mt-6 space-y-2 text-zinc-700 list-decimal list-inside">
        <li>まずは課題や利用シーンをヒアリング（30分〜）</li>
        <li>評価テンプレートを設定し、対象LLM/RAGを接続</li>
        <li>自動で品質データを収集・可視化</li>
        <li>改善提案を確認し、チームで合意形成</li>
      </ol>
      <p className="mt-4 text-zinc-600">
        すぐに試せて、既存システムにも簡単に追加できます。
      </p>
    </div>

    <div>
      <h2 className="text-2xl md:text-3xl font-semibold">成果の出し方</h2>
      <ul className="mt-6 space-y-2 text-zinc-700 list-disc list-inside">
        <li>品質を見える化して「感覚判断」から脱却</li>
        <li>変更の効果を即時に比較し、改善を高速化</li>
        <li>社内外のチームが同じ指標で議論・合意</li>
        <li>継続的な改善サイクルで精度と信頼性を強化</li>
      </ul>
      <p className="mt-4 text-zinc-600">
        難しい設定や専門知識は不要。誰でも運用品質を高められます。
      </p>
    </div>
  </div>
</section>
     

      <section className="px-6 md:px-10 py-16 bg-white">
        <div className="max-w-6xl mx-auto">
          <h2 className="text-2xl md:text-3xl font-semibold text-center">FAQ</h2>
          <ul className="mt-6">
            <li className="py-3 border-b">
              <p className="font-medium">既存のシステムと連携できますか？</p>
              <p className="text-sm text-zinc-600 mt-1">LangChain、LlamaIndex、OpenAI API、主要ベクトルDBなどと連携可能です。</p>
            </li>
            <li className="py-3 border-b">
              <p className="font-medium">導入開始までの期間は？</p>
              <p className="text-sm text-zinc-600 mt-1">要件により異なりますが、最短2週間から利用開始可能です。</p>
            </li>
            <li className="py-3 border-b">
              <p className="font-medium">自社データの安全性は確保されていますか？</p>
              <p className="text-sm text-zinc-600 mt-1">暗号化・分離・権限管理・監査ログにより保護。オンプレミス環境にも対応します。</p>
            </li>
          </ul>
          <div className="mt-10 text-center">
            <a href="/contact" className="px-6 py-3 bg-black text-white rounded-xl text-sm font-medium">デモ・相談を申し込む</a>
          </div>
        </div>
      </section>
    </main>
  );
}
